var config = {};

// twitter keys
config.consumer_key = 'changeme';
config.consumer_secret = 'changeme';
config.access_token_key = 'changeme';
config.access_token_secret = 'changeme';

module.exports = config;
